# M3.5 completion assignment — September 25

Shane explicitly resumed M3.5 with “I said complete it stop failing.” This
assignment supersedes the prior stopped state while retaining the failed
[historical report](M3.5-FINAL-RESULT.md). Complete HOST-248; stop before M4.
GPT-6 Sol medium workers own bounded provider and gate changes. The primary
owns integration, preservation, live verification, publication and handoff.

## Four checkpoints and dependencies

1. **Provider reliability and preservation.** Capture current owner/public/demo
   state. Record failure scenarios before changes; add bounded GET retries and
   sanitized provider evidence. Remove the redundant reverse from final cleanup
   when provider readback already proves the exact original route. Preserve all
   ownership, pending-operation and metadata guards. This does not establish
   the unknown cause of the previous provider read failure.
2. **Focused verification and installation.** Run the real HTTP provider fault
   boundary and exact route-only journey. Require unchanged owner content and
   existing rows. Review the diff, run `make check`, produce the restricted
   build, commit clean source, verify populated backups and retained-binary
   compatibility, and install the immutable candidate. Depends on checkpoint 1.
3. **Repeatable acceptance.** Two fresh full gates must pass on the same clean
   installed source. Capture owner state before each run; compensate only exact
   gate-owned content through guarded APIs; verify public HTML, retained rows,
   services, route and artifact checksums after each. Preserve every failed
   artifact and diagnose any failure before further action; never blind-loop
   the gate or weaken its assertions. Depends on checkpoint 2.
4. **Delivery.** Publish public-safe source to canonical Gitea and GitHub mirror,
   verify CI, record final receipts and current roadmap, complete HOST-248 and
   hand off the protected preview. M4/M5 stay unclaimed. Depends on checkpoint 3.

## Acceptance boundaries

The [provider scenarios](M3.5-PROVIDER-READ-SCENARIOS.md) and
[runner scenarios](../e2e/beta/README.md) define the changed behavior before
acceptance. Fault injection is restricted to GETs at an explicit outbound HTTP
proxy; provider writes are never retried automatically. Real provider route
round trips and full gates verify the unchanged live boundary.

No database migration, reset, destructive restore or customer launch is part of
this change. Existing Rust/application code and schema remain unchanged.
Platform and tenant backups must be verified before installation. Retain the
previous immutable release and all historical evidence. Customer admission and
live payments remain disabled. The owner-facing credential correction already
installed at `d681a41` remains part of both full browser acceptance passes.

## Focused integration evidence

`make check` and the restricted Vite build passed on September 25.
The real route-only probe `complete-route-20260925` passed 14/14 assertions,
including the corrected entry-route cleanup, seven-path HTTPS readiness and
unchanged owner/public/demo content. Receipt SHA-256:
`46ce7ffbd4c9921f26adda3976be96ea77724692d4742e16f7853210e540b89f`.
This focused run permits an uncommitted harness; clean-source acceptance still
requires both full gates after freezing and installing the candidate.

The provider HTTP boundary probe `complete-provider-20260925` passed all 21
assertions: real baseline/final reads plus dropped connection, 503, 429,
exhaustion, definitive refusal, malformed payload, provider rejection,
Retry-After beyond budget and slow-trickle deadline. No write was forwarded;
the private provider journal was byte-identical. Receipt SHA-256:
`17617f501c81bc0c270f5e0a8be440d725596becea9a839129a72cdee15eb2db`.
The primary independently verified both receipt bundles and the final source.
