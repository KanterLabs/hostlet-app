# M3.5 complete — restricted beta preview

**M3.5 is accepted at HOST-248. Stop before M4.** The restricted preview is
live on clean installed `513340ad736667583c037b493f5a5254c0942047`. Two consecutive full runs passed
all 62 assertions and all nine feature phases, including final live-entry route
cleanup. GPT-6 Sol medium workers implemented the bounded fixes; the primary
reviewed the diffs, ran the real boundaries and independently verified receipts.

## What changed

Final route cleanup cuts over directly when readback already proves the exact
original route, avoiding the redundant reverse that preceded the prior failure.
Provider GETs have at most three attempts and a 12-second wall deadline; bounded
Retry-After is respected. Writes remain single-attempt. Safe diagnostics retain
the provider verb, error classification, status and retry counts. Unrelated
apex/wildcard guards remain exact; only the restored beta record treats a null
baseline comment and an empty provider comment as equivalent.

The installed sign-in correction distinguishes rejected credentials from an
expired session. Both full browser runs verified wrong-password rejection,
then successful owner sign-in with the application credentials.

## Full acceptance receipts

All artifact directories below are private under `artifacts/e2e/M3.5/`.
`SHA256SUMS` verifies each payload; the final column records the hash of that
receipt file. The original failed receipts remain unchanged historical evidence.

| Run | UTC on September 25 | Result | SHA256SUMS SHA-256 |
| --- | --- | --- | --- |
| `complete-513340a-full-1` | 18:06:08–18:14:33 | **PASS 62/62** | `9b62d9874273bda3684d4e83904abf536f70815fc0ccfe7d1cdb6d1bc5c366d2` |
| `complete-513340a-full-2` | 18:15:25–18:23:40 | **PASS 62/62** | `007f43d66cebe2325d0624dbf9dcd6306de9d54498ffb8dae1e90b9a2c9b591a` |

Both runs began and ended clean on the same installed source, harness, tools,
fixtures and binaries. They exercised real Chromium, restricted HTTPS/control
APIs, immutable draft saves and reload/restart persistence, PostgreSQL-backed
demo writes, review/approval/publication, access and stale-write protection,
independent static serving during service outages, partial-seed repair,
populated isolated restores, startup recovery, and exact route round trips.
All temporary Chromium profiles and isolated restores were removed.

Each directory includes `REPORT.md`, `manifest.json` and `SHA256SUMS`.
The manifest's `inputs.rerun` is the exact repeat command with private paths and
scoped environment references. Use a fresh run ID and the owner-preservation
procedure before/after each authorized repeat. Verify payloads with
`sha256sum -c SHA256SUMS` from the artifact directory. A gate deliberately
changes synthetic owner content and adds a demo row; it is not a normal-use
walkthrough. The scope and failure inventory remain in
[the completion plan](M3.5-COMPLETION-PLAN.md).

## Preservation and installation

Fresh platform and project backups passed independent SHA-256 and populated
archive checks before installation. Schema remains 6. Rust/application/storage
source and all five binaries match retained `d681a41`; the twelve managed unit
definitions changed only immutable release paths. The connected tunnel was
retained. Backup/install receipts remain in the private preview directory at
`gate-backups/preinstall-513340ad7366/preinstall-receipt.json` and
`managed-switch-513340ad736667583c037b493f5a5254c0942047.json`.

Guarded API compensation after each gate restored the original approved public
content and original private draft through new immutable revisions. Public HTML
is byte-identical to entry, SHA-256
`ab67128ae4424a772579fac19f2504c00bf417a22bb9413ca6ec3557a20c83d7`.
All eight entry demo rows plus both gate-created rows remain: 10 total.
All eleven managed services passed independent readiness readback. No database
reset, live restore or deletion of owner history occurred.

Independent preservation receipts, each with four passing checks:

- `preserved-complete-513340a-full-1`: `95ffdbe79cc8a1604366579690c319b747191eb99ed73a9a6e6faa41768cc142`.
- `preserved-complete-513340a-full-2`: `0193c2f5afbc698ca5bb511339717760344045591f5a8fc54c2e4b8c51cb9f4f`.

Focused route verification passed 14/14; provider HTTP fault checks passed 21/21,
including a slow-trickle deadline and read lock release. Their hashes are in
[the completion plan](M3.5-COMPLETION-PLAN.md). `make check`, restricted Vite
build, script syntax and public-source scans passed. No post-implementation
unit tests were added.

## Delivery and milestone boundary

Canonical Gitea and the public GitHub mirror received the accepted source.
[Candidate CI](https://github.com/KanterLabs/hostlet-app/actions/runs/36173241711) passed for the exact installed commit;
Web checks use `homelab` and Rust checks use `homelab-heavy`. Documentation-only
closure commits do not change the installed candidate or its E2E evidence.
Helm snapshot 12277 records 23 completed cards,
eleven future Backlog cards and no owned active task. All 41 prerequisite links
remain; M4/M5 remain unclaimed. Completing M3.5 does not start M4.

[Demo access](M3.5-DEMO-ACCESS.md) distinguishes the browser gate password from
the application password. This is a restricted synthetic development preview.
Customer admission and live payments remain disabled. Next.js 16 and the
throughput target remain deferred, and host reboot recovery remains unverified.
The [prior failed result](M3.5-FINAL-RESULT.md) remains available with its evidence
limits; the underlying reason for that historical provider read failure is unknown.
