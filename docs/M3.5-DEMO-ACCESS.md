# Hostlet private demo access

The restricted preview is available for Shane's review as of 2026-09-25.
This is the separately authorized demo handoff, not M3.5 acceptance. HOST-248
remains blocked on full recovery acceptance; M4/M5 remain unclaimed.

## Open and try it

Open `https://beta.hostlet.cloud`. The private credential file on the shared
development host is `/home/shane/.config/hostlet-preview/access.json` (0600):

1. Use its `gate.username` and `gate.password` at the browser's access prompt.
2. Use its `application.email` and `application.password` on Hostlet's sign-in form.
3. Edit the introduction, click **Save private preview**, and reload to see it persist.
4. Open `https://beta-demo.hostlet.cloud`, add a journal item and reload.
5. View the approved portfolio at
   `https://beta-portfolio.hostlet.cloud/owned-preview/` and follow a project/demo link.

The same gate credentials protect all three hosts. A private draft edit does
not change the approved portfolio. Credential values are not stored in source,
chat or E2E artifacts.

## Verified evidence

The one scoped Chromium walkthrough passed **21/21 checks**. Fresh owner login,
selected project, actual save POST 201, exact-value reload/API readback, demo
POST 201 and reload/API readback, approved HTML home/detail and demo navigation
all passed. Three anonymous hosts returned 401. All seven protected/anonymous
readiness probes held their required results for twenty continuous seconds.
Independent PostgreSQL readback confirmed the saved draft and demo row, with
the approved publication unchanged. Both run-created sessions were revoked
(HTTP 204), and the run-owned Chromium profile was removed.

- Installed product: `a1ffa60d5e0fc767042247980fda9175328f3ea6`, unchanged.
- Clean walkthrough source: `1098441e25f0096368eb6eb643a5e4682a2fd432`.
- Private artifact: `artifacts/e2e/M3.5/demo-handoff-owner-preview-20260925/`.
- Verified receipt SHA-256:
  `68e70d79916855da2290ffc2720d01c3393f31aec3c686fa323471bb67bc4869`.

The artifact includes the exact repeat command, copied runner/browser source,
source and binary hashes, safe browser events, assertions and cleanup evidence.
Use a new run ID when repeating; the walkthrough adds a synthetic private draft
revision and demo item. It never changes routing, restarts services or publishes
a draft. See [the scenarios](M3.5-DEMO-HANDOFF-SCENARIOS.md).

## Retained state and limits

All eleven existing managed services were ready before cutover. Their installed
binary/web hashes and the private access file were checked. Existing populated
data and verified backups were retained. No rebuild, installation, schema
migration, fixture reseed, service restart, outage drill or restore occurred.
Provider readback verified exactly the three owned preview routes; the legacy
route and private journal remain available for exact rollback with
`scripts/beta/cloudflare.py reverse`. Route rollback preserves current data.

The preview routes are intentionally retained under Shane's later scoped
authorization, overriding the earlier requirement to wait for two full gates
before demo access. Future work must preserve this live preview and owner edits.
The previous full gate is still failed. Recovery sequencing, two clean full
gates and canonical source publication/mirror CI remain outstanding under
HOST-248. Host reboot/boot order remains unverified. This development preview
does not admit customers, collect payments or advance to M4.
