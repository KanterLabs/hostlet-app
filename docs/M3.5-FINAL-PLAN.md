# M3.5 final recovery assignment — 2026-09-25

Shane authorized this plan and execution with “OK MAKE FINAL 3.5 PLAN AND FIX”.
HOST-248 remains the single work card. This resumes M3.5 recovery and its two
full acceptance runs; it does not start M4. The existing restricted preview is
the entry state to preserve. Historical failed receipts remain failed.

## Five stopping points

1. **Preservation and diagnosis.** Record the current release, service policies,
   schema, draft/approval/publication identities, populated data and exact live
   route state in private evidence. Retain fresh verified backups before changing
   the installation. Read-only diagnosis identifies the cumulative manager-start
   budget as the last full-gate failure; no data repair is indicated.
2. **Minimal corrections and focused proof.** Respect the actual manager start
   interval before intentional restart probes, recover every affected owned
   service, and preserve precise safe child failure details. Make the runner
   restore the exact route state found at entry, including an already-live
   preview. Correct the diagnosed misleading sign-in error. Exercise these
   changed paths at real process/HTTP/browser boundaries before acceptance.
3. **Freeze one candidate.** Review worker diffs, run `make check`, build the
   restricted dashboard, verify backups and schema-6/retained-release
   compatibility, then install one immutable clean candidate. No reset,
   reseeding, live restore, weakened access control or start-limit change.
4. **Two full passes.** Run the unchanged required scenario groups twice on the
   same clean candidate with distinct run IDs and fresh browser sessions.
   Independently verify every artifact and cleanup result. Any full-run failure
   means preserve the complete report, finish exact owned cleanup, restore the
   entry preview state, block HOST-248 and STOP. No repair or second attempt
   follows a failed full run in this assignment.
5. **Publish and hand off.** Publish reviewed source to canonical Gitea and its
   GitHub mirror, verify required CI, smoke-check the protected dashboard/demo/
   portfolio, update the handoff and dependency snapshots, and complete HOST-248
   only when all evidence is present. Leave M4/M5 unclaimed.

## Failure inventory, before implementation

| Failure | Required behavior and real-boundary oracle |
| --- | --- |
| Earlier phases consume the three-start manager budget | Read the real policy and observed starts, wait within a finite deadline for capacity, then observe both distinct successful restart timestamps. Never reset the failed state to erase the limiter or enlarge its policy. |
| A failed/automatic start is newer than the last successful start | Include applicable manager timestamps or a conservative observed quiet interval; do not treat an old successful timestamp as proof of available budget. |
| Platform database outage also stops runtime/build/database/publisher workers | Restore the exact database and affected owned units in dependency order, then require all their readiness probes and retained-data readback, including on failure cleanup. |
| Budget/readiness does not recover by the deadline | Fail with safe operation, substep, unit, policy and elapsed-time evidence. No unbounded polling or repeated start loop. |
| Primary action and cleanup both fail | Retain both reasons and the original primary substep. Cleanup failure makes the run fail. |
| Gate begins with the separately authorized preview already live | Verify its exact provider ownership at entry. Still prove reversal to the retained original route and reapplication. Restore the entry live state on success or failure; never leave Shane on the legacy route merely because an older gate did so. |
| Provider mutation succeeds before its command throws | Record intent before mutation and inspect/reconcile the exact owned route in cleanup. Unknown ownership fails closed. |
| Gate writes synthetic narrative/publications while existing owner data exists | Preserve immutable history and private entry snapshots. Use supported revision-checked operations for any content cleanup; never overwrite an unknown concurrent edit, reset a database, or restore a backup over current data. Report retained run-created revisions and rows explicitly. |
| Incorrect application password returns 401 | The actual browser shows “Email or password is incorrect. Please try again.” and stays signed out; a subsequent correct login reaches the existing project and editor. No account-existence disclosure. |
| An authenticated session actually expires, or login fails for another reason | Preserve session-expiry handling and existing server/rate-limit/network messages; no auth bypass, new credential exposure or changed backend contract. |

Focused validation covers a preconsumed restart budget followed by the real
startup/recovery operation, current publication and demo-row preservation,
policy preservation, exact live-entry route round trip/cleanup, and rejected
then successful browser login. Existing unchanged save, publication, restore
and static independence evidence is retained; the two full runs exercise their
combined behavior again. A focused sequence must not quietly become a full
rehearsal under another name.

## Ownership

Use GPT-6 Sol workers at medium reasoning. Worker A owns the startup operation
and its safe diagnostics in `e2e/beta/operations.mjs`. Worker B owns runner
integration, route-entry cleanup, focused login and its README. Worker C owns
only contextual sign-in error presentation and its upfront browser scenarios.
Workers do not start live operations, spawn agents, alter another worker's
files, or add post-implementation unit tests. The primary owns this plan,
integration, serialized builds/installations/drills, backups, route operations,
source publication, independent verification and final tracking.

## Evidence and boundaries

Every focused/full E2E attempt retains a report, manifest, safe observations,
exact rerun inputs, cleanup inventory and external SHA256SUMS receipt. Never
retain passwords, session tokens, request headers or login bodies. No T3
authentication workaround is part of milestone acceptance; real fresh Chromium
checks exercise the deployed public HTTPS boundary.

Preserve the current live preview, owner revisions, databases, legacy route,
retained releases and unrelated resources. Brief owned recovery/route drills
are authorized by this assignment; customer admission, live charging,
purchases and M4 remain outside it. Host boot recovery remains an explicit
unverified limit unless separately exercised; process restarts are not reboot
proof. A plan reduces known risks and does not guarantee a passing result.
