# M3.5 restricted preview scenario and failure inventory

Written before implementation for HOST-246 placement/preservation, HOST-247
durable composition, and the HOST-248 deployed E2E stop. The boundary is a
configured HTTPS entry, real browser and control API, durable platform and
project PostgreSQL, managed processes, an owned Node demo and independent
static portfolio. Use synthetic owned data and the M3 trust boundaries. The
required placement and access choices are inputs described in
[M3.5-PREVIEW-CONTRACT.md](M3.5-PREVIEW-CONTRACT.md); a name in this document is
not evidence of control of a live route. No customer or payment flow is in
scope. All scenarios below require observable behavior, not setup-only checks.

| ID | Setup and action | Pass oracle and retained evidence |
| --- | --- | --- |
| M35-PLACE-01 | Inventory `beta.hostlet.cloud` and old Hostlet ownership; stage an independently owned preview target; run a protected temporary HTTPS cutover and exact reversal within the gate. | Capture non-secret DNS/TLS/route/origin identities and exact prior target; old Hostlet data and process identities remain unchanged. HTTPS reaches only the intended staged target during cutover, and reversal restores the exact prior route. A failed gate restores that route; a final preview route follows two clean gates. Unknown ownership or mismatch stops mutation. |
| M35-ACCESS-01 | From a fresh browser and direct HTTP client, request dashboard, demo, portfolio and sign-up without access, with an expired or malformed session, and as another owner; then sign in as the seeded owner. | Every denied path lacks private data and writes; public sign-up is rejected; the owner reaches all three separately configured HTTPS origins. No network-accessible origin bypasses edge restriction. Private loopback API use by bootstrap/workers remains scoped. Record status, browser URL and redacted request trace without tokens. |
| M35-COMPOSE-01 | Seed one owned account/project, run the exact-source M3 build and release path, load the dashboard in Chromium and open the routed Node demo. | Browser observes the persisted project and a real response from its isolated Node runtime; control reports the exact release and separate project PostgreSQL identity. No fixture-only success payload substitutes for the service response. |
| M35-EDIT-01 | Edit a private narrative field in the browser, write a related project-database row through the demo, restart the managed dashboard/control/demo/database as applicable, then reload. | Both owner edit and database row survive with their IDs and values; session behavior remains coherent; release, approval and static pointers are unchanged unless explicitly changed. Database queries and browser/API observations agree. |
| M35-APPROVAL-01 | Review the exact saved owner revision in the browser, approve it, publish, and follow its project/demo links. Repeat with stale revision, another account and an unapproved field. | The restricted site contains only reviewed fields and correct configured HTTPS links; stale/cross-owner/unapproved requests cannot change the last good site. Publication does not grant anonymous access. A new deployment changes only explicitly authorized managed facts and marks demo readiness for recheck. |
| M35-STATIC-01 | Load home and project detail in fresh authorized Chromium, stop dashboard/control, source-provider fixture and demo endpoints, then load again in a fresh authorized browser. | Both pages and their declared assets load solely from the independent static origin with no dependent request after edge access is granted. Anonymous access still fails. Demo link may be unavailable while stopped; static content, approval and project slot count remain intact. |
| M35-SEED-01 | Re-run bootstrap after an owner edit, then interrupt a separate owned bootstrap between durable steps and rerun it. | Stable owner/project/source IDs remain unique, edits and approval remain intact, no extra slot or duplicate release appears, and incomplete state either converges or reports a bounded repair action. |
| M35-RECOVER-01 | Back up populated owned platform and project stores, restore to isolated targets and compare values, relationships and app read/write. Exercise the exact preview route reversal from M35-PLACE against the same retained populated stores. If a schema/storage format upgrade or deployment change affects populated state, first attempt it with missing/corrupt backup, then perform verified backup and isolated populated compatibility trial. | Real backup and isolated restore pass, ordinary writes remain usable, preview rollback preserves the populated stores without rewind. A qualifying populated change is denied without verified backup/trial and proves retained-binary compatibility. If no qualifying change occurs, record those migration-specific checks `N/A` with evidence that schema/storage format stayed fixed. |
| M35-START-01 | Inject bounded startup/health failures and repeated managed restart, including publisher and database unavailability. | Failure has a finite attempt/deadline and owner-visible reason; no unhealthy target is advertised or published. Last good route/static artifact and durable data survive; recovery uses the same owned identities or a recorded replacement. |
| M35-CLEAN-01 | Finish successful and failed runs, including an interrupted setup. | Exact temporary VMs, processes, namespaces, databases and files are removed only after ownership checks. Inventory and evidence name every retained preview service, database, backup and artifact. Old Hostlet resources are untouched. |

## Failure inventory and coverage

**M35-DB-CLOCK-01 (real-clock provision).** With the preview control using its
normal wall clock (`generation=0`), enqueue one owned project PostgreSQL
provision and run the real database worker once. The scheduler tick may have a
later timestamp than the preceding clock read; the worker must accept that
normal advancement, claim and complete the exact provision operation, and the
owner API must report `ready` with the same database generation and container
identity. An invalid clock schema, a pinned (`generation>0`) tick with a
different time, or a worker/control failure must remain visible and must not
report readiness. Repeat after managed restart to verify retained identity.
This is covered at the M35-COMPOSE/START E2E boundary; no isolated unit test is
needed. A scheduler-only success event or a precreated container is insufficient.

The table above is the E2E inventory required by [TESTING.md](../TESTING.md).
Malformed host, path, session and bootstrap inputs, plus wrong-owner requests,
are covered by M35-PLACE/ACCESS/SEED. Duplicate bootstrap, approval, publish,
restart and route actions, including concurrent/stale completions, must preserve
one durable pointer and the owner's edits in M35-SEED/APPROVAL/START. Timeouts,
cancellation and backpressure become bounded failure receipts in M35-START; no
sleep or generic HTTP 200 is an oracle. M35-EDIT/RECOVER cover transactions,
populated backup/isolated restore and persistence through restart; upgrade
checks apply only to a qualifying populated-state change.
M35-CLEAN covers partial failure and leaks. If a deterministic internal property
proves impossible to observe at these real boundaries, record the exact gap and
write a failing isolated check before its implementation under TESTING.md's
narrow exception; this inventory grants no retroactive unit-test exception.

## Reused M3 evidence

M3.5 does not change release promotion policy or application rollback eligibility.
The two accepted [M3 gate receipts](M3-HANDOFF.md) remain evidence for failed
candidate retention and eligible runtime rollback against current data. Repeating
those unchanged scenarios would require additional builds and runtime capability
measurements unrelated to preview deployment. This scope clarification precedes
any M3.5 full gate; it does not waive a failed test. M3.5 directly exercises the
new managed restart, real populated backup/isolated restore, independent static
serving, access boundary and exact preview route reversal. A later change to
release policy would require its relevant E2E regression coverage.

## Gate evidence

Create a private `artifacts/e2e/M3.5/<run-id>/` record before setup, including
failed, interrupted or blocked attempts. Record exact source and harness
identities, toolchain/image/fixture digests, non-secret placement inputs,
prerequisite health, exact rerun command, each assertion result, browser and
database observations, redacted logs, route reversal and exact cleanup. Include
`manifest.json`, readable `REPORT.md`, and external `SHA256SUMS` over retained
payloads. Verify the receipt independently and record its own hash in the
handoff. A missing or incomplete artifact cannot pass. Run the full gate twice
cleanly on the same implementation commit, serially, after `make check`.
Retain and report a full-gate failure; stop after its cleanup to diagnose it
before another gate. M3.5 proof remains a restricted owned preview, not
customer production readiness. Stop at HOST-248; do not claim M4.
